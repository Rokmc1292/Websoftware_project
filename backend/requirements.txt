# requirements.txt — 이 프로젝트에 필요한 Python 패키지 목록
# 설치 명령어: pip install -r requirements.txt

flask==3.0.3                # 웹 서버 프레임워크 — URL 라우팅, 요청/응답 처리의 핵심
flask-sqlalchemy==3.1.1     # Flask와 SQLAlchemy 연동 — Python 클래스를 DB 테이블로 매핑 (ORM)
flask-migrate==4.0.7        # DB 스키마 변경사항을 버전으로 관리 (git처럼 DB 변경 이력 추적)
flask-jwt-extended==4.6.0   # JWT(JSON Web Token) 발급 및 검증 — 로그인 상태 유지에 사용
flask-cors==4.0.1           # CORS 허용 — React(5173포트) ↔ Flask(5000포트) 간 통신 허용
pymysql==1.1.1              # Python에서 MySQL 데이터베이스에 접속하는 드라이버
cryptography==42.0.8        # pymysql SSL 연결에 필요한 암호화 라이브러리
python-dotenv==1.0.1        # .env 파일의 환경변수를 Python 코드에서 읽어올 수 있게 해줌
anthropic==0.28.0           # Claude AI API 공식 Python SDK — AI 코칭 기능에 사용
bcrypt==4.1.3               # 비밀번호를 안전하게 암호화(해싱)하는 라이브러리 — 평문 저장 금지
google-generativeai==0.8.5  # Google Gemini API SDK — 식단 AI 이미지 분석/코치 기능에 사용
